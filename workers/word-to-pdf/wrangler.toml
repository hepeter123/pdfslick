name = "pdfslick-word-to-pdf"
main = "worker.js"
compatibility_date = "2024-12-01"

# Non-secret environment variables
[vars]
# Comma-separated allowed origins for CORS.
# Set to your production domain(s). Leave empty during development to allow all.
ALLOWED_ORIGINS = ""

# Secret variables — set via CLI, NOT here:
#   npx wrangler secret put CONVERTAPI_SECRET
